pseudo routine:
function sumOfNnumbers(limit)
 set sum = 0
  for i from 1 to limit do
    sum = sum + i
  return sum

pseudo test routine:
function testSumOfNnumbers(){
  input limit 
  sum = sumOfNnumbers(limit)
  print(sum)
}

drver code:
function __main__(){
   testSumOfNnumbers()
}
